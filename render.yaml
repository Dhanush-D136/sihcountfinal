services:
  - type: web
    name: sihcountdown
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
      - key: SECRET_KEY
        generateValue: true
      - key: ADMIN_USERNAME
        value: Vel Tech SIH
      - key: ADMIN_PASSWORD
        value: veltechsmarthack123
